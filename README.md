# scaling-project

Angular starter scaffold added by assistant.

Getting started:

```bash
git clone https://github.com/plasticdevon/scaling-project.git
cd scaling-project
npm install
npm start
```

Notes:
- This is a minimal Angular scaffold. Install versions in package.json may be adjusted.
- If you prefer a different Angular version or an Nx/mono-repo layout, tell me and I can re-scaffold.
